# Keep the default shell native so Linux/WSLg builds use the host GL/EGL
# libraries. Reproducible Windows builds enter the flake explicitly through
# the Windows Make targets, for example: make build-windows
